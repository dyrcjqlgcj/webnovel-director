# 执行派发

把已通过闸门的章节队列转换为写作任务包，优先交给 inkos 或其他执行器。它不判断大方向，只负责让执行器拿到足够明确的输入。

## 解决的问题

- 避免「一句话 prompt 裸写正文」。
- 让每章写作都读取 premise、state、truth、last_audit。
- 保证写后能回写状态并触发审查。

## 什么时候用

- outline-gate PASS 后。
- 用户要求续写指定章节。
- cron 自动写作到点后。
- 修复方案已通过，需要重写某章/某段。
