# 项目初始化反例

## 反例 1：直接写正文

用户：开一本别人通关我刷怪。
助手：好的，第一章如下……

为什么错：没有 premise、禁飞区、chapter_queue，后续无法防偏。

## 反例 2：覆盖旧项目

用户：给已有项目接入导演系统。
助手：重建目录并清空旧大纲。

为什么错：初始化只能补 director/truth 层，不能破坏已有资产。

## 反例 3：premise 过大

把世界观、人物小传、所有副本设定都塞进 `templates\premise.md`。

为什么错：premise 是命题约束，不是百科；太大后每次写前读取会稀释关键约束。
